package Controller;
/*package Model;

import Controller.State;
import Controller.StateController;

public class SimulationMode implements State {
	
	private boolean success=true;
	
	SimulationMode(){
		
		for (int i=0; i< StateController.desiredOutputs.size(); i++){
			if (StateController.desiredOutputs.get(i) != StateController.getCircuit().getOutput(i).getValue()){
				success=false;
			}
		}
	}
	
	public boolean isSuccess(){return success;}
	
	

	@Override
	public void changeState() {
		StateController.setCurrentState(StateController.levelSelect);

	}

}
*/
